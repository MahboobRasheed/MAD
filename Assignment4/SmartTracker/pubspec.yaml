name: smarttracker
description: SmartTracker - GPS + Camera + REST sync demo
publish_to: 'none'
version: 0.0.1
environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  http: ^0.13.6
  uuid: ^3.0.6
  geolocator: ^9.0.2
  image_picker: ^0.8.7+4
  google_maps_flutter: ^2.2.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
